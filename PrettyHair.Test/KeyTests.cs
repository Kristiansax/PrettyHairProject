using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using PrettyHair.Core.KeyGenerators;
using PrettyHair.Core.Interfaces;
using Moq;

namespace PrettyHair.Test
{
    [TestClass]
    public class KeyTests
    {
        [TestMethod]
        public void RandomKeyGenerator()
        {
           /* Mock<RandomKeyGenerator> mockView = new Mock<RandomKeyGenerator>();
            RandomKeyGenerator view = mockView.Object;

            ViewModel viewModel = new CustomerViewModel(mockView.Object, mockContainer.Object);
            viewModel.CustomersRepository = new CustomersRepository();
            viewModel.Customer = new Mock<Customer>().Object;

            Assert.AreEqual(2, );*/
        }
    }
}
